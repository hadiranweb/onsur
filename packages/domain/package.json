{
  "name": "@element-plus/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain layer for Element Plus. No framework/runtime dependencies.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@element-plus/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
